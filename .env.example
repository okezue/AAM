# Closed-model generators. Model IDs are deliberately supplied by the runner,
# because provider model catalogs change over time.
OPENAI_API_KEY=
OPENAI_MODEL=
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=
GEMINI_API_KEY=
GEMINI_MODEL=

# Optional Hugging Face access for gated model weights.
HF_TOKEN=

# Optional run tracking.
WANDB_API_KEY=
